{
  // Deploy ArthaRoshni to Cloudflare Pages (project: artharoshani → https://artharoshani.pages.dev).
  // Manual deploy:  npx wrangler login   (once)   then   npx wrangler pages deploy
  // Or connect the GitHub repo in the Cloudflare dashboard for auto-deploy on every push.
  "name": "artharoshani",
  "compatibility_date": "2025-01-01",
  "pages_build_output_dir": "."
}
